spring.application.name=survey

# =============================================
# DB ?? - H2 ???? (??/????)
# MySQL ?? ???? ?? ?? ??
# =============================================
spring.datasource.url=jdbc:h2:mem:surveydb;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 ?? (?????? DB ??: http://localhost:8080/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA - ? ?? ? ??? ?? ?? + sql/schema.sql ??
spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:sql/schema.sql

# =============================================
# MySQL ?? (???? ? H2 ?? ?? ?? ? ??)
# =============================================
#spring.datasource.url=jdbc:mysql://localhost:3306/survey_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.username=root
#spring.datasource.password=1234

# =============================================
# MyBatis ??
# =============================================
mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.configuration.map-underscore-to-camel-case=true

# =============================================
# Thymeleaf ?? (?? ? ?? OFF)
# =============================================
spring.thymeleaf.cache=false

# =============================================
# ?? ?? (???)
# =============================================
logging.level.com.woongjin.survey=DEBUG
logging.level.org.springframework.security=DEBUG
